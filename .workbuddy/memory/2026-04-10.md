# 2026-04-10

## 19:26 — Y Daily 健康检查逻辑改进
- **问题**: health_check.py 只看 git commit 时间不看新闻内容时间，导致新闻停更但 commit 还在阈值内时误判 OK。update_breaking.py 的 LLM 没有时间窗口过滤，会把旧闻当新闻塞进去。
- **修复 update_breaking.py**: 读取上次 breakingDate 作为时间下界传给 LLM prompt，严格只选该时间之后发布的新闻；无新内容时不更新时间戳。
- **修复 health_check.py**: 新增 `get_content_age()` 解析 index.html 中 breakingDate 实际时间，取 `max(commit_age, content_age)` 作为有效年龄判断。
