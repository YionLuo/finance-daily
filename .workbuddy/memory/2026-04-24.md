# 2026-04-24

## 13:45 - Y Daily Health Check 执行
- Breaking News 检测为 **STALE**（123min，阈值 120min）
- Finance Daily / AI Daily 未到检查时间（22:00 CST），已跳过
- **自动恢复失败**：两个 API 端点均不可用
  - `once.novai.su/v1` → 503 Service Unavailable
  - `api.deepseek.com` → 401 Authorization Required
- 脚本以 exit code 1 退出，等待下次重试

## 14:00 - DeepSeek V4 升级评估
- V4 于 2026-04-23 发布，两个型号：`deepseek-v4-flash`（轻量）、`deepseek-v4-pro`（高性能，对标 GPT-5.5）
- API endpoint / OpenAI SDK 兼容性**不变**，仍 `api.deepseek.com`
- 老模型名 `deepseek-chat` / `deepseek-reasoner` **将于 2026-07-24 弃用**，当前自动映射到 v4-flash（非思考/思考模式）
- 网站当前用法：`utils.py`、`update_ai.py`、`update_finance.py`、`update_breaking.py`、`update_deep_research.py` 都通过 `LLM_MODEL` / `WRITER_MODEL` 环境变量注入模型名
- 建议：
  - 日报类（breaking/ai/finance）→ `deepseek-v4-flash`（平替）
  - Deep Research 写报告 → `deepseek-v4-pro`（真正值得升级，成本约 GPT-5.5 的 1/8）
- 用户尚未确认是否改代码默认值，仅给出环境变量方案
- 14:52 - 已执行代码升级，5 个脚本全部替换完成
  - `utils.py`: `DEFAULT_LLM_MODEL` → `deepseek-v4-flash`
  - `update_ai.py`: `LLM_MODEL` → `deepseek/deepseek-v4-flash`
  - `update_breaking.py`: `LLM_MODEL` → `deepseek-v4-flash`
  - `update_finance.py`: `LLM_MODEL` → `deepseek/deepseek-v4-flash`
  - `update_deep_research.py`: `LLM_MODEL` → `deepseek-v4-flash`，`WRITER_MODEL` → `deepseek-v4-pro`
  - `utils.py` 过期注释 `deepseek-v3.2` 同步修正为 `deepseek-v4-flash`
  - 已验证旧模型名（`deepseek-chat`/`deepseek-reasoner`/`deepseek-r1`）在 scripts/ 目录中无残留

## 17:46 - Y Daily Health Check 执行（第二次）
- Breaking News: **OK** — 最后提交 39min 前，在阈值内
- Finance Daily / AI Daily: 未到检查时间（22:00 CST），跳过
- 总结: **ALL OK**，无需恢复

## 21:47 - Y Daily Health Check 执行（第三次）
- Breaking News: **STALE**（150min，阈值 120min）
- Finance Daily / AI Daily: 未到检查时间（22:00 CST + grace），跳过
- **自动恢复失败**：两个 API 端点均不可用
  - `once.novai.su/v1` → 503 Service Unavailable
  - `api.deepseek.com` → 401 Authorization Required
- 脚本以 exit code 1 退出，等待下次重试
