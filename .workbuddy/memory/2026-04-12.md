# 2026-04-12

## 00:30 — Y Daily 日报停更诊断

- **现象**: 金融日报停更4天（最后4/8），AI日报停更6天（最后4/6），Breaking News 正常
- **根因**: `once.novai.su` API 代理出问题：
  1. SSL 证书不匹配 — CDN 证书 SAN 不含 `once.novai.su`
  2. 后端推理服务 524 超时 — 模型列表可达但推理请求全部超时
- **已修复**: 为三个脚本 (update_finance/update_ai/update_breaking) 加入 `SKIP_SSL_VERIFY` 环境变量支持，绕过 SSL 问题
- **待处理**: novai.su 后端超时需联系排查，或换用新的 API 代理端点
